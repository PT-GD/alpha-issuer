apiVersion: certmanager.alpha-issuer.io/api
kind: AlphaIssuer
metadata:
  labels:
    app.kubernetes.io/name: alpha-issuer
    app.kubernetes.io/managed-by: kustomize
  name: alphaissuer-sample
spec:
  # TODO(user): Add fields here
